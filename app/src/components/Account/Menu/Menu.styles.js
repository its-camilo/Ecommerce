import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  titleItem: {
    fontWeight: "bold",
    color: "#000",
  },
  titleLogoutItem: {
    fontWeight: "bold",
    color: "#BB0000",
  },
});
