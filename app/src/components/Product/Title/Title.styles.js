import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 10,
        paddingBottom: 0,
    },

    title: {
        fontSize: 20,
        fontWeight: "bold",
        marginBottom: 20,
    },
});
