import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 20,
    },

    searchText: {
        fontSize: 20,
        fontWeight: "bold",
    },

    otherText: {
        fontSize: 14,
        paddingTop: 5,
    }
});
