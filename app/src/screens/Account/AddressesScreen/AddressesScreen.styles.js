import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 20,
  },

  loading: {
    marginTop: 20,
  },

  noAddressText: {
    textAlign: "center",
    paddingTop: 20,
    fontSize: 18,
  },
});