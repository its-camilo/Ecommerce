import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 10,
  },

  noOrders: {
    fontSize: 18,
    textAlign: 'center',
    paddingTop: 20,
  },

  title: {
    fontSize: 20,
  },
});
